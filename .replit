run = "cd frontend && npm install && npm run build && cd ../backend && pip install -r requirements.txt && python -m uvicorn main:app --host 0.0.0.0 --port 8000"

[languages.python]
version = "python3"

[languages.nodejs]
version = "node18"
